# Stress Lens Data Symmetrizer

A Python script that generates a complete 3D stress dataset by applying symmetry to partially processed lens stress data.

The script reads a semi-processed stress data file, mirrors the data across symmetry axes, and produces a full stress dataset.

## Usage

Run the script:

python Symmetrize_stressdata.py

The generated symmetric stress data will be saved in the `output` folder.
